# Meet.XLM

- `documentation.md`: visão do produto e MVP no Stellar.
- `frontend/`: landing page Next.js inspirada na POAP (mint/drop/connect).

Para rodar a landing:
```bash
cd frontend
npm install
npm run dev
# http://localhost:3000
```
